apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cluster-scoped RBAC for the pruner ServiceAccount. Consumed (with a namePrefix
# and a subjects[].namespace patch) by overlays/prod/cluster/ only.
resources:
  - pruner.clusterrole.yaml
  - pruner.clusterrolebinding.yaml
