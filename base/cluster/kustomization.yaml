apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cluster-scoped RBAC for the manager ServiceAccount. Consumed (with a
# namePrefix and a subjects[].namespace patch) by overlays/<env>/cluster/.
resources:
  - manager.clusterrole.yaml
  - manager.clusterrolebinding.yaml
